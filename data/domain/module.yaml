product:
  type: lib
  platforms: [android, iosArm64, iosSimulatorArm64, iosX64]

dependencies:
  - ../base-lib
  - ../model
  - ../repo


settings:
  android:
    namespace: dev.shuanghua.weather.domain
